const expect = require('chai').expect

describe('Dummy test for husky. In init repo something need to pass tests.', () => {
    it('should return true', () => {
        const test = true
        expect(test).to.be.ok
    })
})
